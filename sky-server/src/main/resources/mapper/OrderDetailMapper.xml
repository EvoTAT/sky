<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">

    <insert id="insertBatch">
        insert into order_detail (order_id, dish_id, setmeal_id, dish_flavor, name, number, amount) values
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId},#{item.dishId},#{item.setmealId},#{item.dishFlavor},#{item.name},#{item.number},#{item.amount})
        </foreach>
    </insert>
    <select id="getByOrderId" resultType="com.sky.entity.OrderDetail" parameterType="long">
        SELECT * FROM order_detail WHERE order_id = #{orderId}
    </select>
</mapper>
