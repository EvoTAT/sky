<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <insert id="insertBatch">
        INSERT INTO setmeal_dish (setmeal_id, dish_id, name, copies)
        VALUES
        <foreach collection="setmealDishes" item="item" separator=",">
            (#{item.setmealId}, #{item.dishId}, #{item.name}, #{item.copies})
        </foreach>
    </insert>



    <select id="getSetmealIdsByDishId" resultType="java.lang.Long">
        SELECT setmeal_id FROM setmeal_dish WHERE dish_id IN
        <foreach item="dishId" collection="list" separator="," open="(" close=")">
            #{dishId}
        </foreach>
    </select>
</mapper>
